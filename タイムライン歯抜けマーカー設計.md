# タイムライン歯抜けマーカー設計

ウィンドウ非アクティブや WebSocket 切断でタイムラインに生じる「未取得区間 (歯抜け)」を、ギャップマーカーとして保持し、その場で補給できるようにする設計。

## 0. 依存ブランチ (前提)

本設計は MISTEMS フォーク (`mistems-main`) に既に入っている次の 2 つのパッチを前提にしており、**そのままでは本家 misskey-dev/misskey には入らない**。最終的な投入先は MISTEMS フォークなので問題ない。

| 依存パッチ | コミット | 本設計が依存している内容 |
| --- | --- | --- |
| タイムマシン | `24ce423` タイムマシンとふぁぼった | `TimeshiftPaginator` の「別 Ref に持って表示用 computed で合成する」構造、`FusionNote` / `isFusionNote` の型設計、`MkPagination` の `customItems`、`use-timemachine-page.ts` の `loadNewer` にあるスクロール位置復元処理 |
| FTTL の歯抜けバグ対策 | `4a7868c` FTTLの歯抜けバグ修正 | `FanoutTimelineEndpointService` の 2 段目 safety net (Redis 上の飛び石歯抜けを `sinceId` / `untilId` 全範囲で DB から補完する処理)。これが無いと `sinceId` + `untilId` の区間取得が Redis 側の欠損をそのまま返し、マーカーを埋めても穴が残る |

本家へ持っていく場合は、前者は合成 computed と挿入メソッドだけを切り出せば独立できるが、後者は backend 側の補完処理を別途入れる必要がある。

## 1. 解決したいペインポイント

1. **未取得のノートが残っていることに気付けない**
   - 再接続後に新着が 1 件も来ない静かな TL では、キューも「新しいノート」ボタンも出ず、欠損の存在を示す UI が無い
2. **未取得区間のノートを見る手段がタイムライン全体のリロードしかない**
   - `fetchNewer` は `getNewestId()` を境界にするため、キューやストリームで新着が先に入ると欠損区間を飛び越える

「30 件までなら自動で補給する」だけの修正は 1 も 2 も解決しない (緩和にとどまる) ため、**欠損を状態として保持するマーカー方式は必須**とする。自動補給はマーカー方式の中の 1 ステップとして吸収する。

## 2. 現状の調査結果

### 2.1 歯抜けの発生源

| # | 発生源 | 箇所 | 補足 |
| --- | --- | --- | --- |
| A | WS 再接続後に欠損区間を取りに行かない | `packages/misskey-js/src/streaming.ts` `onOpen()` | 再接続時はチャンネルを `connect` し直すだけ。`MkStreamingNotesTimeline.vue` で `fetchNewer` を呼ぶのはポーリングモードの経路のみ |
| B | キュー上限で最古が捨てられる | `packages/frontend/src/utility/paginator.ts` `enqueue()` / `fetchNewer({toQueue})` | 100 件超過で `pop()` / `slice(0, 100)` により最古側を落とす |
| C | 切断が無音 | `packages/frontend/src/preferences/def.ts` `serverDisconnectedBehavior` | 既定値 `quiet`。`main-boot.ts` の `_disconnected_` ハンドラは reload / dialog のときしか動かない |

### 2.2 trim による自己回復と、その限界

`releaseQueue()` → `unshiftItems()` → `trim()` で items は新しい 30 件 (`MAX_ITEMS`) に切られる。

- キューが **30 件以上** あれば旧セグメントは丸ごと消え、以後の `fetchOlder` は最古 id から連続するので歯抜けは見えない
- キューが **30 件未満** (静かな TL / 短時間の切断) だと、旧セグメントと新セグメントが同居し、その間の欠損が **目に見える歯抜け** として残る
- ポーリングモードは `sinceId` 昇順で順次追いつくため、構造上は恒久的な歯抜けが起きない

つまり実害があるのは **リアルタイムモードで、切断により数件〜数十件を失い、復帰後の新着が 30 件未満** のケース。

### 2.3 既存構造で流用できるもの

| 流用元 | 内容 | 本設計での使い方 |
| --- | --- | --- |
| `MisskeyEntity` (`paginator.ts`) | `{ id, createdAt, _shouldInsertAd_? }` | マーカーは `id` / `createdAt` を持たせて同じ制約を満たす |
| タイムマシン `TimeshiftPaginator` (`utility/timeshiftPaginator.ts`) | `timeshiftItems` / `fusionItems` を **別 Ref** に持ち、`customItems` で MkPagination へ渡す | マーカーも別 Ref に持ち、表示用 computed で合成する |
| `FusionNote` / `isFusionNote` | Note を extends した型と type guard | `TimelineGap` / `isTimelineGap` の型設計をそのまま踏襲 |
| `isPausingUpdate` / `queuedAheadItemsCount` (`MkStreamingNotesTimeline.vue`) | 非アクティブ検知とキュー表示 | トリガー判定と UI ブロックの実装位置 |
| backend `sinceId` + `untilId` 同時指定 | `FanoutTimelineService.getMulti` が `id < untilId && id > sinceId` の排他境界で返す | 区間取得に使用。antenna / list / channel / role / mentions を含む全 TL エンドポイントが境界指定を受け付ける。**ただし並び順は endpoint ごとに異なる** (`notes/mentions` は sinceId があれば常に昇順、`roles/notes` は常に降順) ので、frontend 側で id ソートして使う |

## 3. 設計方針

### 3.1 型: items は触らず、マーカーを別 Ref に持つ

`IPaginator<T, _T = T & MisskeyEntity>` の `items: Note[]` は変更しない。理由:

- `getNewestId()` / `getOldestId()` は items の id をソートして境界にするため、マーカーを混ぜると id 設計と除外フィルタが必要になる
- `_shouldInsertAd_` や `FusionNote._isRealtime` のように **ノートへ属性を生やす方式** は、trim / `removeItem` / 重複除去でノートが出入りするたびに状態が消える (タイムマシンで経験済)
- 合成 computed を読むのは `MkStreamingNotesTimeline.vue` のテンプレだけなので、`MkNotesTimeline.vue` など他の利用者に型変更が波及しない

```ts
// packages/frontend/src/utility/paginator.ts (または timeline-gap.ts)
export type TimelineGap = {
	id: string;              // `gap:${sinceId}` など表示 key 用
	createdAt: string;       // MisskeyEntity と同じ形にするためだけに持つ (セパレータ判定には使わない、3.7 / 4 参照)
	_type: 'gap';
	sinceId: string;         // 旧セグメント側の境界 (この id より新しいものが未取得)
	untilId: string | null;  // 新セグメント側の境界。null は「ここから現在まで」
	fetching: boolean;
};

export function isTimelineGap(item: Misskey.entities.Note | TimelineGap): item is TimelineGap {
	return '_type' in item && item._type === 'gap';
}
```

表示側:

```ts
// MkStreamingNotesTimeline.vue
const gaps = ref<TimelineGap[]>([]); // 深い ref にする (shallowRef だと gap.fetching の変更が UI に届かない)

const displayItems = computed<(Misskey.entities.Note | TimelineGap)[]>(() => {
	// items を走査し、gap.sinceId に一致するノートの「直前」に gap を差し込む
	// sinceId のノートが items に無い gap は、untilId が null (現在まで) なら先頭、それ以外は描画しない
});
```

補足ルール:

- **key は生成時に採番して固定する** (`gap:${counter}` 等)。`sinceId` / `untilId` は補給のたびに進むので key に含めると TransitionGroup が毎回 remount してしまう
- **位置は sinceId 側 (旧セグメント最新ノート) の直前** の 1 点で決める。untilId 側ノートの存在は問わない。再接続時は `getNewestId()` がキュー側の id を返しうるため sinceId ノートが items に無い期間があるが、`untilId == null` なら先頭に出すことで「新着が来ない静かな TL」でもマーカーが見える
- gaps は Paginator の中ではなく **コンポーネント側 (または `useTimelineGaps(paginator)` composable)** に持つ。Paginator への変更は挿入メソッドの追加だけに留める
- **同じ sinceId のマーカーは 1 つに統合する** (`openGap` / `onNoteRemoved` の付け替え時)。区間は広い方を採用し、`untilId = null` (現在まで) は確定済の `untilId` より広いとみなす
- **孤立したマーカーは掃除する**: `untilId` 確定済で sinceId ノートが items に無く、かつ sinceId が items の最新より古い (= trim で押し出された) マーカーは、items の変更を watch して捨てる。sinceId が items の最新より新しいものは先読みキューにいる (解放で戻る) ので残す

### 3.2 マーカーの生成トリガー

visibility 単独では欠損の証拠にならないので、次の 3 つをトリガーにする。

| トリガー | 検知方法 | マーカーの初期値 |
| --- | --- | --- |
| WS 再接続 | `stream.on('_disconnected_')` でフラグを立て、次の `_connected_` で判定 | `sinceId = getNewestId()`, `untilId = null` |
| キュー溢れ | `enqueue()` / `fetchNewer({toQueue})` で `MAX_QUEUE_ITEMS` を超えた瞬間に Paginator の `onQueueOverflow(oldestRemainingQueuedId)` フックが呼ばれる | `sinceId = items[0].id`, `untilId = 捨てた直後のキュー最古 id` |
| 長時間 hidden からの復帰 (安全網) | `useDocumentVisibility` で hidden 時刻を記録し、visible 復帰時に閾値 (例: 5 分) 超過なら。**リアルタイムモード限定** (ポーリングモードはキュー経由で順次追いつくため対象外) | `sinceId = getNewestId()`, `untilId = null` |

- 再接続をトリガーにするのは、**新着が来なくてもマーカーが出る** ようにするため (ペインポイント 1)
- hidden 復帰の安全網は、タブ凍結中にソケットが黙って死に `close` が遅れて届くケースの保険。自動補給で空が返れば即消える

`untilId = null` のマーカーは、次にストリームから届いた最初のノート (`prepend` / `enqueue`) の id で `untilId` を確定させる。

### 3.3 区間の取得 (補給)

タイムスタンプではなく **id** を境界にする。backend は `sinceDate` / `untilDate` を `idService.gen()` で id に変換しているだけで、id ならノート境界そのものを排他で指せる。

```ts
// composables/use-timeline-gaps.ts (抜粋)
async function fill(gap: TimelineGap) {
	if (gap.fetching) return;
	if (!hasItem(gap.sinceId)) {
		// sinceId のノートが先読みキューにいる可能性。「現在まで」のマーカーは先頭に出ているので解放して取り込む
		if (gap.untilId == null) paginator.releaseQueue();
		if (!hasItem(gap.sinceId)) { remove(gap); return; } // trim で旧セグメントごと消えている
	}

	gap.fetching = true;
	const res = await paginator.fetchRange({ sinceId: gap.sinceId, untilId: gap.untilId, limit: 30 });
	gap.fetching = false;
	if (res == null) return; // 失敗時はマーカーを残す (再試行可能)
	if (res.length === 0) { remove(gap); return; }

	// backend の並び順は endpoint ごとに異なるので信用せず、id で新しい順に揃える
	const descending = res.toSorted((a, b) => a.id < b.id ? 1 : a.id > b.id ? -1 : 0);
	// 取得分は常に sinceId ノードの直前 (= マーカーの位置) に入る
	const inserted = paginator.insertItemsBefore(gap.sinceId, descending);

	if (gap.untilId != null) gap.untilId = descending.at(-1)!.id; // 新しい側から埋めた → 残りは取得した最古より下
	else gap.sinceId = descending[0]!.id;                          // 古い側から埋めた → 残りは取得した最新より上

	if (res.length < 30 || inserted === 0) { remove(gap); return; }
	await nextTick();
	// 続きが見えていて、自動補給してよい状況 (先頭表示中かつアクティブ) で、連続回数が上限 (3) 未満なら続行
	if (isGapVisible(gap) && canAutoFill() && gap.autoContinueCount++ < MAX_AUTO_CONTINUE) void fillPage(gap);
}
```

`Paginator` には末尾 / 先頭への追加しか無いため、`insertItemsBefore(anchorId, items)` を追加する (id 重複除去 + `triggerRef`、戻り値は実際に挿入した件数)。区間取得は endpoint / params が Paginator の private なので `fetchRange({ sinceId, untilId, limit })` を Paginator 側に追加し、通常の fetch と同じパラメータ組み立てを再利用する。挿入位置は上限確定済でも「現在まで」でも **sinceId ノードの直前** の 1 箇所で済む (取得分は必ず両境界の間にあり、マーカーは sinceId ノードの直前に描画されるため、上限確定済の補給ではマーカーが取得分の下へ押し下げられて Mastodon の LoadGap と同じ読み進め方になる)。

補給時の注意:

- **終了条件は「limit 未満」または「新規挿入 0 件」の OR** にする。reload 直後などで区間のノートが既に items にある場合、limit ちょうどが返り続けても挿入 0 件で打ち切れる
- **アンカーが items に無いとき** (fetch 中に trim / 削除された) は結果を捨ててマーカーも消す。アンカー消失は旧セグメントが trim されたことを意味し、以後は `fetchOlder` が連続するため安全
- **スクロール位置**: 補給ノートは常にマーカーの下側 (untilId ノートとマーカーの間、または マーカーと sinceId ノートの間) に入るので、ユーザーがマーカーをクリックした場合は視点の直下に新ノートが現れる。一方、生成直後の自動補給でユーザーがスクロール中だと viewport 上方の高さが変わる。Chrome / Firefox は `overflow-anchor` 既定値で補正されるが Safari は補正されない。自動補給は `isTop()` のときだけ即時実行し、それ以外はマーカー表示に留める。保留した自動補給は、visible 復帰 / 「新しいノート」ボタン / スクロールで先頭に戻った時のいずれでも再試行する (必要なら `use-timemachine-page.ts` の `loadNewer` にあるアンカー計測 → `nextTick` + 2 回 `requestAnimationFrame` → `scrollTop` 補正を切り出して使う)

### 3.4 繰り返し (ギャップがなくなるまで)

- 取得件数が `limit` 未満 → マーカー削除
- `limit` ちょうど → 境界 id を進めてマーカーを残す
- 自動継続には上限 (連続 3 回 = 90 件) を設け、超えたらマーカーを残して手動クリック待ちにする。手動クリックで回数をリセットする。大規模な切断の復旧直後に全クライアントが延々と取得し続ける (サンダリングハード) のを防ぐため
- 自動継続の前に `canAutoFill()` (先頭表示中かつアクティブ) も再確認する。矩形上「見えている」だけのバックグラウンドタブで走り続けないため
- **`v-appear` だけでは止まる場合がある。** `directives/appear.ts` は IntersectionObserver の交差「変化」でしか発火せず、500ms throttle もかかる。下向き補給ではマーカーが 30 件分押し下げられて一度画面外に出るので再発火するが、`untilId == null` の上向き補給ではマーカーが動かず画面内に残り続けて発火しない。補給完了後にマーカー要素の `getBoundingClientRect()` でコンテナ内に見えているか判定し、見えていれば自前で次の `fillGap` を呼ぶ (再帰は `fetching` フラグで直列化)

### 3.5 生成直後の自動補給

マーカー生成時に `fillGap` を 1 回だけ即時実行する。

- 30 件未満で埋まれば **ユーザーが気付く前に消える** (軽い離席では何も出ない)
- 30 件返れば残りを示すマーカーとして表示に残る

これにより「マーカーが出ている = 未取得がある」を 1 対 1 に保つ。常にブロックを見せたい場合に備え、自動補給を切る設定を後から足せる構造にしておく。

### 3.6 trim / removeItem との関係

- trim で旧セグメント (sinceId 側ノート) が消えたら、そのマーカーは捨ててよい。以後は `fetchOlder` が最古 id から連続するため未取得の穴は残らない
- マーカーが生きている間は両端のノートが items に存在するので、差し込み位置を id で一意に決められる
- `removeItem` で sinceId ノートが削除された場合は、その 1 つ下 (古い側) のノート id に `sinceId` を付け替える。削除済ノートは戻ってこないので区間が広がっても実害はない
- **`init()` / `reload()` で items が丸ごと差し替わるときは gaps も空にする。** 最新 15 件を取り直すので区間は連続しており、古いマーカーが残ると「sinceId ノードが偶然 15 件内にある」ケースで無意味なマーカーが出る。`reloadTimeline()` と MkError の retry、`computedParams` 変更の 3 経路すべてが `paginator.init()` を通るので、`paginator.fetching` の立ち上がりを `watch` して一括で消すのが漏れない

## 3.7 リアクティビティ検証結果

`MkStreamingNotesTimeline.vue` の paginator は `markRaw` + `useShallowRef: true` で、items は in-place 変更 (`unshift` / `push` / `splice`) の後に `triggerRef` する、あるいは `trim()` で `slice` を代入する、という更新経路になっている。computed 合成がこの経路で正しく動くかを vue 3.5.38 (リポジトリの lock と同一) で実機検証した。

| 操作 | computed の挙動 | 判定 |
| --- | --- | --- |
| items を in-place `unshift` (trigger なし) | 古い値のまま | 想定どおり。Paginator は必ず `triggerRef` を呼ぶので実害なし |
| `triggerRef(items)` | 再計算され新要素を含む | OK |
| `items.value = items.value.slice(0, 30)` (trim) | 参照が変わるので再計算 | OK |
| 深い `ref` の gaps に push | 再計算され gap が差し込まれる | OK |
| `gap.untilId` / `gap.sinceId` を直接代入 | 再計算され位置が動く | OK (`shallowRef` にすると届かない) |
| `computed` を `watch` し、`push` + `triggerRef` | 1 回だけ発火 | 二重発火なし |

つまり **Paginator 側の更新経路を一切変えずに computed 合成で成立する**。in-place 変更と代入が混在していても、全経路の末尾で `triggerRef` または代入が走っているため、computed は常に最新の items を読める。

一方で、テンプレ側に index 参照や分岐順など「items が Note[] であること」を前提にした箇所があり、それらが今回の穴として 3.1 / 3.3 / 3.4 / 3.6 / 4 に反映されている。要約:

1. gap の key を境界 id から作らない (remount 防止)
2. 日付セパレータの `paginator.items.value[i - 1]` を gap を飛ばす helper に置換
3. テンプレ分岐で gap 判定を先頭に置く
4. `init()` で gaps を空にする
5. 終了条件に「新規挿入 0 件」を加える
6. `v-appear` の再発火に頼らず、補給後に可視判定して続ける
7. 自動補給は `isTop()` のときだけ即時実行する (Safari のスクロール補正なし対策)

MkNote は `deepClone(props.note)` で受け取った時点の複製を持つため、computed が毎回新しい配列を返しても、key (`note.id`) が同じなら remount も内部状態の破壊も起きない。

## 4. UI

`MkStreamingNotesTimeline.vue` の `v-for` 内で `isTimelineGap(item)` のとき、ノートの代わりに次のブロックを描画する。

- 文言: 「間のノートを取得」(新規 locale キー、`locales/ja-JP.yml` のみ編集)
- 取得中は `MkLoading :inline="true"`
- **テンプレの分岐順は gap 判定を先頭に置く。** 現在の `v-if` 日付セパレータ / `v-else-if` 広告 / `v-else` の前に `isTimelineGap(item)` を入れないと、gap が `note` として MkNote に渡る
- **日付セパレータの前要素参照を直す。** 現在は `paginator.items.value[i - 1]` と index 参照しているが、`displayItems` を回す以上 `i - 1` が gap になりうる。「直前のノート」を gap を飛ばして取る helper (`prevNote(i)`) に置き換え、gap 自体はセパレータ判定に参加させない。gap の `createdAt` は型制約を満たすためだけに持つ
- `data-scroll-anchor` は gap には付けない (`use-scroll-position-keeper.ts` はノート id で復元するため)
- スタイルは既存の `.more` / `.date` に揃える

## 5. 変更ファイル (実装済)

| ファイル | 変更内容 |
| --- | --- |
| `packages/frontend/src/utility/paginator.ts` | `getNewestId` を public 化、`fetchRange` / `insertItemsBefore` / `onQueueOverflow` を追加 (interface `IPaginator` にも追加) |
| `packages/frontend/src/composables/use-timeline-gaps.ts` (新規) | `TimelineGap` 型 / `isTimelineGap` / `useTimelineGaps(paginator, options)`: gaps 状態、`displayItems` 合成、`openGap` / `onQueueOverflow` / `bindUntil` / `fill` / `tryAutoFill` / `onNoteRemoved`、`fetching` 立ち上がりでの reset |
| `packages/frontend/src/components/MkStreamingNotesTimeline.vue` | `displayItems` を回すテンプレ (gap 分岐を先頭に)、`getPrevNote` による日付セパレータ判定、再接続 / hidden 復帰 (5 分) / キュー溢れの 3 トリガー、`prepend` での `bindUntil`、`noteDeleted` での再アンカー、マーカーのスタイル |
| `packages/frontend/test/use-timeline-gaps.test.ts` (新規) | 実 `Paginator` + `fetchRange` モックによる composable の単体テスト (合成位置 / 統合 / 溢れ検知 / 両方向の補給 / backend の並び順に依存しないこと / 自動継続の上限と中断 / 孤立マーカーの掃除 / 終了条件 / 失敗時 / 再アンカーと統合 / reset) |
| `packages/frontend/test/streaming-notes-timeline-gaps.test.ts` (新規) | `MkStreamingNotesTimeline.vue` のコンポーネントテスト (stream / store / misskey-api / visibility をモック): 再接続でマーカーが先頭に立ち自動補給すること、初回接続では立てないこと、届いた最初のノートで上限確定しセパレータがマーカーを飛ばすこと、hidden 5 分の閾値、補給完了でマーカーが消えること |
| `locales/ja-JP.yml` | `fetchNotesBetween: "間のノートを取得"` |
| `CHANGELOG.md` | `## Unreleased` → `### Client` に Enhance 1 行 |

backend 変更なし。`MkNotesTimeline.vue` (非ストリーミング) は対象外。

## 6. 実装状況

1. ~~`paginator.ts` に型と挿入メソッドを追加~~ 済
2. ~~`MkStreamingNotesTimeline.vue` に gaps 状態と `displayItems` 合成、UI ブロック~~ 済 (composable に切り出し)
3. ~~再接続トリガー + 自動補給~~ 済
4. ~~キュー溢れトリガー、hidden 復帰の安全網~~ 済
5. ~~補給後の可視判定による自動継続、`removeItem` 時の再アンカー~~ 済 (`v-appear` は使わず可視判定のみ)

未実施: 実機 (ブラウザ) での動作確認。単体テストと typecheck / eslint は通過済。

## 6.1 フルレビュー (5 ペルソナ並列) の結果と対応

ADDF の `addf-code-review-agent` 戦略に従い skeptic / attacker / newcomer / maintainer / domain-skeptic の 5 ペルソナで並列レビューし、集約ルール (同一指摘の統合、2 ペルソナ以上の一致で重要度 1 段上げ) で整理した。

| 指摘 (統合後) | 指摘者 | 補正後 | 対応 |
| --- | --- | --- | --- |
| `fill()` が backend の並び順を前提にしており、`notes/mentions` (常に昇順) と `roles/notes` (常に降順) で挿入順と境界更新が逆転する | maintainer, attacker | Critical | id ソートに変更。テスト追加 |
| 自動継続が `canAutoFill()` を再確認せず、上限も無い (バックグラウンドタブや復旧直後の一斉取得で走り続ける) | attacker (+ maintainer の items 肥大) | Critical | `canAutoFill()` 再確認 + 連続 3 回の上限。テスト追加 |
| スクロールで先頭に戻る経路 (`onScrollContainerScroll`) だけ `tryAutoFill` を呼ばず、キュー溢れ由来のマーカーが自動補給されない | skeptic, maintainer, domain-skeptic, attacker | Critical (補正) | `tryAutoFill` を追加 |
| trim で sinceId ノートが押し出された上限確定済マーカーが `gaps` に残り続ける | maintainer, domain-skeptic | Critical (補正) | items の watch で孤立マーカーを掃除。テスト追加 |
| `getNewestItemId` (items のみ) と `paginator.getNewestId` (キュー優先) が紛らわしい | newcomer, skeptic, domain-skeptic | Critical (補正、実害は命名) | `getNewestDisplayedItemId` に改名し使い分けをコメント |
| `onNoteRemoved` → `removeItem` の順序依存が明示されていない | newcomer, attacker | Critical (補正、実害はコメント) | JSDoc と呼び出し側コメント |
| コンポーネント側 (再接続 / hidden / `getPrevNote`) のテストが無い | skeptic, maintainer (+ PR の claude-review) | Critical (補正) | `streaming-notes-timeline-gaps.test.ts` を追加 (再接続 / 初回接続 / 上限確定とセパレータ / hidden 閾値 / 補給完了) |
| hidden 復帰の閾値 5 分が未決のまま固定値 | skeptic, maintainer, domain-skeptic | Warning (補正) | 「仮の値」とコメント。設定化は未決のまま |
| `fetchRange` が backend の補完処理 (FTTL 歯抜け対策) に依存していることがコードに無い | skeptic, maintainer | Warning (補正) | `fetchRange` の JSDoc に明記 |
| `noteRemovedFromAntenna` 経路で再アンカーが漏れている | domain-skeptic | Warning | `onNoteRemoved` を追加 |
| `onNoteRemoved` の付け替え先に既存マーカーがあると sinceId が重複する | skeptic | Warning | 付け替え時に統合。テスト追加 |
| hidden 復帰トリガーがポーリングモードでも発火し、欠損が無いのにマーカーが出うる | skeptic | Warning | `stream != null` で限定 |
| `GAP_FETCH_LIMIT` が Paginator の定数と偶然一致しているだけ | maintainer | Warning | `SECOND_FETCH_LIMIT` を export して共有 |
| `TimeshiftPaginator` に新メソッドが無条件継承され、表示 Ref と食い違う | maintainer | Warning | 併用非対応をコメント |
| `insertItemsBefore` が trim しない | maintainer | (却下) | 既存の `fetchOlder` も trim せず、items は無限スクロールで元々 30 件を超える。自動継続の上限で肥大は抑制 |
| 設計書の不整合 (3.1 の createdAt コメント、2.3 の「全 TL 対応済」、統合ルール未記載) | domain-skeptic, maintainer | Suggestion | 本書を訂正 |
| 命名 (`openGap` / `bindUntil`)、トリガー関係の説明、`.gap` / `.ad` の CSS 重複、`getPrevNote` の戻り値型、テストヘルパーのコメント | newcomer | Suggestion | コメント追加、CSS 共有、型修正、コメント修正。改名は見送り |
| 手動クリック時の `releaseQueue` が scrollToTop を伴わない、`onQueueOverflow` が単一スロット、`bindUntil` が全「現在まで」マーカーに束縛 | domain-skeptic, attacker | Suggestion | 見送り (現状の呼び出し経路では実害なし)。`bindUntil` は不変条件をコメントに明記 |

PR (#54) の claude-review による追加指摘:

| 指摘 | 補正後 | 対応 |
| --- | --- | --- |
| `.gap` / `.ad` の CSS 共有パッチで置換が新ブロックの内側にマッチし、`.ad, .ad:empty { display: none; }` となって広告が常に非表示になっていた | Critical | `.ad, .gap` の共通スタイルと `.ad:empty` を復元 |
| `insertItemsBefore` / `fetchRange` / `onQueueOverflow` が `order === 'newest'` を暗黙に前提にしている (`'oldest'` は MkDrive / modlog で使用中) | Warning | JSDoc に前提を明記し、`useTimelineGaps` は `'oldest'` の paginator を渡されたら throw する |
| 設計書のルート直置き | Suggestion | オーナー判断で現状維持 |

## 7. 未決事項

- hidden 復帰の閾値 (仮: 5 分)
- 自動補給を切る設定を初回から入れるか
- `_disconnected_` 時に `serverDisconnectedBehavior: quiet` でも stream-indicator 等で軽く通知するか (本設計の範囲外)
